# دليل رفع وربط منصة Devisly على استضافة Hostinger Web وقاعدة بيانات MySQL 🚀

تم تجهيز المشروع بالكامل ليكون جاهزاً للرفع المباشر على **Hostinger Web Hosting (hPanel)** والربط بقاعدة بيانات MySQL في أقل من 5 دقائق!

---

## 📦 الملفات الجاهزة في مجلد المشروع:

1. 🗜️ **حزمة الموقع الجاهزة للرفع (ZIP):**  
   👉 **`devisly_hostinger_ready.zip`** *(تحتوي على ملفات React + API + .htaccess)*
2. 🗄️ **ملف قاعدة البيانات (SQL):**  
   👉 **`hostinger_database.sql`** *(الجداول الرسمية والبيانات المرجعية)*

---

## 🛠️ خطوات الرفع والتشغيل خطوة بخطوة:

### الخطوة 1: إنشاء قاعدة البيانات في Hostinger (hPanel)
1. ادخل إلى حسابك في **Hostinger (hPanel)**.
2. اذهب إلى قسم **Databases (قواعد البيانات)** -> **MySQL Databases**.
3. قم بإنشاء قاعدة بيانات جديدة:
   - **Database Name (اسم القاعدة):** مثال `u123456789_devisly`
   - **Username (اسم المستخدم):** مثال `u123456789_admin`
   - **Password (كلمة المرور):** اكتب كلمة مرور قوية واحفظها.
4. اضغط على **Create (إنشاء)**.

---

### الخطوة 2: استيراد الجداول في phpMyAdmin
1. بجانب قاعدة البيانات التي أنشأتها، اضغط على زر **Enter phpMyAdmin**.
2. من القائمة العلوية في phpMyAdmin، اضغط على تبويب **Import (استيراد)**.
3. اضغط على **Choose File (اختيار ملف)** واختر ملف:
   📁 **`hostinger_database.sql`**
4. اضغط على **Go (انطلاق)** في أسفل الصفحة ليتم إنشاء جميع الجداول بنجاح.

---

### الخطوة 3: رفع ملفات الموقع إلى `public_html`
1. في Hostinger hPanel، اذهب إلى **Files (الملفات)** -> **File Manager (مدير الملفات)**.
2. ادخل إلى مجلد **`public_html`**.
3. اضغط على زر **Upload (رفع)** في الأعلى، واختر الملف المضغوط:
   📦 **`devisly_hostinger_ready.zip`**
4. بعد اكتمال الرفع، اضغط بزر الفأرة الأيمن على الملف واختر **Extract (فك الضغط)** داخل `public_html`.

---

### الخطوة 4: ضبط معلومات الاتصال في `api/config.php`
1. داخل مجلد `public_html/api/`، افتح ملف **`config.php`** واضغط على Edit (تعديل).
2. ضع معلومات قاعدة البيانات التي أنشأتها في الخطوة 1:

```php
define('DB_HOST', 'localhost');                  // اتركه localhost
define('DB_NAME', 'اسم_قاعدة_البيانات_في_هوستينغر'); // مثال: u123456789_devisly
define('DB_USER', 'اسم_المستخدم_في_هوستينغر');      // مثال: u123456789_admin
define('DB_PASS', 'كلمة_المرور_التي_اخترتها');       // مثال: YourPassword123#
```

3. احفظ الملف (Save).

---

### الخطوة 5: التحقق والتشغيل 🌐
- افتح دومين موقعك (مثال: `https://yourdomain.com`).
- ستجد أن منصة **Devisly** تعمل مباشرة بكامل ميزاتها مع حفظ واسترجاع البيانات من قاعدة بيانات MySQL على هوستينغر!
- يمكنك فحص اتصال الـ API بزيارة: `https://yourdomain.com/api/index.php?action=status`.
